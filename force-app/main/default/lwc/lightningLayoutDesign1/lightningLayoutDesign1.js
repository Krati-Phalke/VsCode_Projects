import { LightningElement } from 'lwc';
import submitIcon from '@salesforce/resourceUrl/submitIcon';

export default class LightningLayoutDesign1 extends LightningElement {

    submitIcon = submitIcon;
}